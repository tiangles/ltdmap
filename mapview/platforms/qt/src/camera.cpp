#include "camera.h"

using namespace ltd_mapview;


Camera CameraFactory::scrollCamera(const Camera &cam, int x, int y)
{
    Camera result;


    return result;
}

Camera CameraFactory::createCamera(const Camera &cam, BoundingBox &bb)
{

}
